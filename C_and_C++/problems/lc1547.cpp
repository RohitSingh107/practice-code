#include <climits>
#include <vector>
#pragma GCC optimize("Ofast")
#pragma GCC target("sse,sse2,sse3,ssse3,sse4,popcnt,abm,mmx,avx,avx2,fma")
#pragma GCC optimize("unroll-loops")
#include <bits/stdc++.h>

using namespace std;

int cost(int length_to_cut, vector<int> &cuts) {

  int ans = INT_MAX;

  for (int i =)

    return ans;
}

int minCost(int n, vector<int> &cuts) { return cost(n, 0, cuts); }

int32_t main() {
  clock_t _t = clock();

  cerr << "Run Time: " << (double)(clock() - _t) / CLOCKS_PER_SEC << " seconds";
  return 0;
}
