#include <bits/stdc++.h>
#include<optional>
#include <string.h>
using namespace std;
int main(){
	std::cout<<"text"<<std::endl;
	string s = "This is text";
	std::cout<<s.size()<<endl;
	s.size();
	map<int,int> mp;
	s.size();
	mp.insert({4,9});
	cout<<mp.size()<<endl;
	return 0;
}
